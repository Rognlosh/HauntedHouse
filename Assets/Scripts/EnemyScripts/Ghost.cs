using UnityEngine;

/// <summary>
/// Призрак — преследует игрока сквозь стены.
/// Поведение идентично базовому Enemy, но NavMeshAgent настроен
/// на тип агента "Ghost" и использует отдельный NavMesh Surface,
/// который покрывает стены. Всё это настраивается в инспекторе,
/// код переопределять не нужно.
/// </summary>
public class Ghost : Enemy
{
    // Логика преследования и блуждания — из базового Enemy.
    // Прохождение сквозь стены обеспечивается NavMesh с типом "Ghost".
    // Ограничение по этажам — границами NavMesh Surface на сцене.
}